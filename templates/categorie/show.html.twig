{% extends 'backbase.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block contentback %}
    <h1 style="text-align: center; margin-bottom: 1em; color: #00A8B3">Catégorie</h1>

    <table class="table" border = 2>
{#        <tbody> #}
{#            <tr>#}
{#                <th>IdCategorie</th>#}
{#                <td>{{ categorie.idCategorie }}</td>#}
{#            </tr>#}
            <tr>
                <th>Nom Catégorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
{#        </tbody>#}
    </table>

    <a href="{{ path('app_categorie_index') }}">Retourner à la liste des Catégories</a>

    <a href="{{ path('app_categorie_edit', {'idCategorie': categorie.idCategorie}) }}">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
