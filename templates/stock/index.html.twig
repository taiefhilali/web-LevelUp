{% extends 'backbase.html.twig' %}

{% block title %}Stock index{% endblock %}

{% block contentback %}
    <h1>Stock index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Quantite</th>
                <th>Etat</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for stock in stocks|reverse %}
            <tr>
                <td>{{ stock.id.idProduit }}</td>
                <td>{{ stock.nom }}</td>
                <td>{{ stock.quantite }}</td>
                <td>{{ stock.etat }}</td>

                <td>
                    <a href="{{ path('app_stock_show', {'id': stock.id.idProduit}) }}">show</a>
                    <a href="{{ path('app_stock_edit', {'id': stock.id.idProduit}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_stock_new') }}">Create new</a>
{% endblock %}
