<form method="post" action="{{ path('app_livreur_delete', {'idUser': livreur.idUser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livreur.idUser) }}">
    <button class="btn">Delete</button>
</form>
