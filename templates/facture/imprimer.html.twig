{% block body %}
    <h1 align ="center">Facture</h1><hr>


    <table class="table table-striped table-bordered" >
        <thead>
        <tr>
            <th>Date</th>
            <th>prixTotal</th>


        </tr>
        </thead>
        <tbody>
        {% for fact in facture  %}
            <tr>

                <td>{{ fact.date ? fact.date|date('Y-m-d') : '' }}</td>
                <td>{{fact.prixTotal}}</td>

                <td class="text-right">
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}