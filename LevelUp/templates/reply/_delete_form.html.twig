<form method="post" action="{{ path('app_reply_delete', {'idr': reply.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reply.idr) }}">
    <button class="btn">Delete</button>
</form>
