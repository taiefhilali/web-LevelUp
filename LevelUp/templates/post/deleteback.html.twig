
{% extends 'backbase.html.twig' %}

<form method="post" action="{{ path('app_post_deleteback', {'id': post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('deleteback' ~ post.id) }}">
    <button type="submit" class="btn btn-info">Supprimer</button>
</form>
