{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Sexe</th>
                <td>{{ client.sexe }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}">back to list</a>

    <a href="{{ path('app_client_edit', {'idUser': client.idUser}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
