<form method="post" action="{{ path('app_stock_delete', {'id': stock.id.idProduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock.id.idProduit) }}">
    <button class="btn">Delete</button>
</form>
